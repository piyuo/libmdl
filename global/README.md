# global model

``` bash
Account : every client must create one account, we bill by account, bill every one month

  + Store : account can own multiple store. every store have different domain name and product
    - Location : every store can have multiple location
  + User : account can have multiple user

```
